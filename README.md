# RunCrew

Group running scheduler. Create or join running groups, schedule runs, view them by week or list.

### How to run locally

1. Run the following command to serve the app locally:

   ```
   npx serve .
   ```

   This will start a local server (by default at http://localhost:3000) so you can use the app in your browser.
